#!/usr/bin/bash
NAMESPACE=${1:-jupyter-jh-v4-test}
echo "Namespace: $NAMESPACE"

last_pod="$(kubectl get pods -n "$NAMESPACE" | grep jupyterhub | grep Running | cut -f1 -d' ' | tail -1)"
kubectl logs -n "$NAMESPACE" -f "$@" "$last_pod"
